apiVersion: v2
name: specdoc
description: HedgeDoc-based spec review stack (editor via the adfinis/hedgedoc subchart, postgres, nightly backup, and the spec board).
type: application
version: 0.1.0
appVersion: "1.11.0"
dependencies:
  - name: hedgedoc
    version: "0.6.0"
    repository: https://charts.adfinis.com
    condition: hedgedoc.enabled
